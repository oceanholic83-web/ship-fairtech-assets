# 작업 로그

시간순 추가 전용. **위에 쌓지 말고 아래에 붙인다.**

형식은 `## [YYYY-MM-DD] 구분 | 제목`.

```
grep "^## \[" claude/log.md | tail -5
```

구분: `작업` / `측정` / `감사` / `정정` / `결정` / `조사` / `발행`

한 항목은 **3~5줄**. 길어지면 해당 원장(STATUS / SITE_REFERENCE / CONTENT_LEDGER / FACT_SOURCES)에 쓰고
여기엔 한 줄 요약과 어디를 보라는 것만 남긴다.

---

## [2026-07-16경] 정정 | robots.txt 차단(617)이 역효과였음
- World Cup 잔재 URL을 색인에서 빼려고 robots.txt로 막았음. **정반대 결과**
- 크롤이 막히면 구글은 그 URL을 **재평가하지 못해 색인에 남긴다.** 10일 낭비
- 조치: 617 비활성화 + 301(406)로 전환. **다시 켜지 말 것**

## [2026-07-26] 작업 | 항만 가이드 페이지 봇 가시성 확보
- 문제: 페이지 정적 텍스트가 **109자.** 나머지는 전부 JS가 그림. 봇에게는 빈 페이지였다
- `build.js` 에 `buildDirectoryHtml()` 신설 → `data.js` 의 기관 데이터를 빌드타임에 HTML로 굽는다
- 결과: 정적 텍스트 109 → **8,684자**, 외부 링크 0 → **97개**, 기관 0 → **99곳**(10개 `<details>` 그룹)
- 오더: `orders/order-port-guide-rewrite-260726.md`, `order-port-guide-directory-260726.md`

## [2026-07-26] 정정 | 내 오더의 검증 단계가 틀렸음
- 「`fct-pg-off` grep → no matches」로 적었는데 템플릿 CSS에 선택자가 남아 매칭됨. **대표가 잡아냄**
- 오더 2에서 죽은 CSS를 지우고 정정
- 교훈: 검증 명령은 내가 머릿속으로 만든 기대값이 아니라 **실제 산출물 기준**으로 쓴다

## [2026-07-26] 작업 | CDN 핀 자동화 — 낡은 커밋 고정 구멍 막음
- `config.json` 의 `loaderUrl` 에 `@<commit>` 이 손으로 박혀 있었다. data.js를 고쳐도 옛 커밋이 서빙됨
- `build.js` 에 `gitHead()` / `dataIsDirty()` / `resolveLoaderUrl()` 추가 → 빌드 시 git HEAD로 자동 치환
- `data.js` 가 커밋 안 된 상태면 경고. **커밋 → 푸시 → 빌드 순서**가 강제된다

## [2026-07-26] 작업 | 붙여넣기 누락 감지 (buildStamp)
- 구조상 구멍: 빌드 산출물을 **사람이 워드프레스에 붙여넣는다.** 안 붙여넣어도 아무도 모른다
- `template.html` 최상단에 `<!-- faircast-port-guide data-rev:{{dataRev}} built:{{buildDate}} -->`
- `site-monitor.js` 의 `checkBuildStamp()` 가 라이브 `data-rev` 와 로컬 `data.js` sha256(8자리)을 대조
- **경보가 실제로 울리는지 검증함**: data.js에 빈 줄 추가 → CRITICAL exit 1 → `git checkout` → 정상

## [2026-07-26] 정정 | 카테고리 noindex CRITICAL은 오탐이었다 (주의)
- 모니터가 「529 스니펫 작동 안 함」 CRITICAL. **거짓이었다**
- 원인: 워드프레스가 `max-image-preview:large` robots 메타를 **자체 출력**하고 스니펫이 **두 번째**를 추가한다. 첫 태그만 읽어 오판
- 조치: `robotsContent($)` 로 전체 robots 메타를 합쳐 읽도록 수정. 호출부 3곳
- 결과: 인프라 CRITICAL 1 → **0**, 정상 69. v7 미해결 질문 하나 종결

## [2026-07-26] 정정 | 좌표 버그는 없었다
- 「울산·제주 지도에서 보기 버튼이 죽었다」고 단정. `data-lat/lng` 가 비어 있어서
- `port-atlas.js` 를 읽어보니 **폴백이 이미 있었다** (`PORTS.find(...)` → `port.coords`)
- 예정했던 오더 3 취소. **코드를 읽기 전에 버그를 선언하지 않는다**

## [2026-07-26] 결정 | 도선·항해 수치 데이터(ops) 계획 폐기
- 대표가 갑판부가 아니라 검증 불가. 검증 못 하는 숫자를 실무자 독자에게 내보내는 건 신뢰성 자살
- Search Console 실측 검색어(`vetting 뜻`, `p&i club`, `용선료`, `유조선 크기 분류`)는 **스펙이 아니라 개념**을 찾는다
- 방향: 기관 디렉토리·개념 해설로. `ops-draft-3ports.js` 는 미배선 상태로 둠

## [2026-07-26] 작업 | 「12개 무역항」 표현 전 파일 제거
- 국가관리무역항은 14개, 전체 무역항은 31개(14 국가관리 + 17 지방관리). 12는 틀린 수
- `config.json` `description`·`badge`, `template.html`, `loader.js` 인트로 문구까지 정리

## [2026-07-26] 정정 | 군산항 초안의 사실 오류 (발행 전 차단)
- 묘박지 용량을 **DWT로 읽었다. 실제 단위는 GT.** A-0 = 50,000 GT × 10척, A-4 = 7,000 GT × 1척
- 관청 문장에 따옴표를 씌워 인용했다 → 지침 [11] **인용구 0개 원칙** 위반. 서술형으로 재작성
- 부두 소개가 관청 문장 순서(1→7)를 그대로 따라가 모방 위험 → **선박 크기 순**(7부두부터)으로 재구성
- 상세는 `CONTENT_LEDGER.md`

## [2026-08-03] 작업 | 작업 원장 신설 (`claude/`)
- 자매 사이트(faircall) 원장 6종을 검토해 faircast에 필요한 5종으로 재구성
- faircall의 실패 지점은 「빌드 오류」였지만 **faircast는 「사실 오류」와 「검증 도구 오판」**이다. 그래서 문서 구성이 다르다
- `FACT_SOURCES.md` 가 faircast 고유. 1차 출처가 열리는지/막히는지를 남긴다
- **전제 미충족: device bridge 미연결.** STATUS 7장 참조

## [2026-08-04] 정정 | web_fetch가 faircast.kr에 먹힌다 — 원장이 틀렸다
- `SITE_REFERENCE` 5장에 「Wordfence가 막아 web_fetch 불가」로 박혀 있었으나 **거짓**
- robots.txt·홈·About·개별 글 전부 정상 조회됨. 라이브 검증을 Claude가 직접 할 수 있다
- 신뢰도 표에서 web_fetch를 5순위 「사용 불가」 → **2순위권**으로 올림
- 교훈: 도구 제약도 원장에 박아두면 검증 없이 몇 주가 흐른다

## [2026-08-04] 측정 | 색인 역행 확인 — 34 → 24
- 색인 24 / 색인 안 됨 129. 6/5 이후 54 → 34 → 24 단조 감소
- 클릭·노출 28일 기준 급락. 8/15 목표(색인 40+) 산술적으로 도달 불가
- **「가치 없는 콘텐츠」가 포괄 코드일 뿐이라는 해석을 버린다.** 검색 쪽에서도 같은 판정이 87건 나와 있다
- 「크롤링됨-색인안됨」 87개를 분해: 축구 잔재 27 / WP 기술 URL 7 / 필수 페이지·허브 13 / 실제 글 38

## [2026-08-04] 결정 | 축구 URL 301 → 410 Gone
- 실측: `/simulate/france`, `/tournament`, `/insights/mbappe-*` 전부 200으로 홈 내용 반환 = 홈 301
- **관련 없는 URL 대량을 홈으로 보내면 구글은 soft 404로 본다.** 색인에서 빠지는 경로가 없었다
- 617(robots 차단)이 색인에 「가둔」 것이라면 406은 홈에 「묶어둔」 것. 형태만 다른 같은 실수
- WPCode 406 전면 교체 → `status_header(410)`. 검증: 축구 URL 4개 410, 홈·카테고리 정상
- 축구 URL 마지막 크롤이 5월. 재크롤 전까지 유효성 검사 누르지 않는다

## [2026-08-04] 작업 | feed·페이지네이션 색인 제외 (WPCode 신규)
- 대상: `/feed/`, `/comments/feed/`, 태그 feed 2종, `/page/2/`, `/page/4/`
- `X-Robots-Tag: noindex, follow` **HTTP 헤더**로 처리. 메타 태그로 붙이면 529·358과 겹쳐 7/26 오판 구조를 재현한다
- `is_paged()` 가 이 사이트에서 안 잡혀 경로 정규식(`#/page/[0-9]+/?$#`)으로 교체 후 통과
- `wp-emoji-release.min.js` 는 정적 파일이라 PHP 스니펫이 안 탄다. 이번엔 제외
- 오염 검사: 홈·About·개별 글에 noindex 없음 확인

## [2026-08-04] 정정 | 「12개 무역항」이 워드프레스에 살아 있었다
- 7/26 로그에 「전 파일 제거」로 적혀 있었으나 **레포만 일부 정리됐고 About·홈·378 메타는 그대로**였다
- 실제 수치: 국가관리무역항 14 / 전체 31 / `data.js` 수록 13 (광양·여수 분리)
- 조치: `pages/about.html` 3곳, `pages/homepage/template.html` 2곳 → 「주요 무역항」·「13 수록 무역항」. 커밋 9bf70ef
- 워드프레스 About·홈 붙여넣기 + WPCode 378의 `522 =>` 메타까지 반영. 라이브 확인 완료
- **교훈: 레포 수정 ≠ 사이트 반영.** 붙여넣기 단계가 빠지면 로그만 완료로 남는다

## [2026-08-04] 정정 | 초장문 한 줄 HTML은 모델이 손으로 고치면 깨진다
- `about.html` 은 전체가 한 줄. 편집 도구가 줄을 통째로 재작성하다가 치환 3곳 중 1곳만 반영 + 블록 중복 발생
- 거절하고 문자열 치환 스크립트(`fs.split().join()`)로 재처리 → 정상
- **규칙: `pages/*.html` 은 손편집 금지. 스크립트 치환만.**

## [2026-08-04] 정정 | content-audit 결과 두 건이 오진이었다
- **「E-E-A-T 4점 미만 80/80편, 저자 표기 없음」 → 거짓.** 라이브 확인 결과 `Faircast 편집팀` 바이라인, About the Editorial Desk 박스, 발행일+수정일, 출처, 관련 글 전부 있다. 573 스니펫 도입 전 데이터이거나 채점 오류
- **「fairwayeta 대비 본문 비율 80% 미만 = 중복 의심 8편」 → 과장.** 그 지표는 길이만 잰다. 중복도와 상관이 없다
- 매칭 자체도 엉터리: `korea-shipbuilding` ↔ `hormuz-crisis`, `eu-ets` ↔ `uae-quits-opec` 로 짝지어져 있었다
- 8쌍을 직접 대조: 3쌍은 아예 다른 글, 2쌍은 골격 일부만 공유, 번역에 가까운 건 톤수·빙급·선원유기 3편. **언어가 달라 구글 중복 판정 대상이 아니다**
- 유일하게 걸리는 건 `maritime-labour-abandonment-foc-2025` — 오프닝 일화가 영문판과 문장 단위로 같다

## [2026-08-04] 발견 | 진짜 중복은 사이트 내부에 있었다 — 900/570 프레임 3편
- `vlcc-fleet-900-effective-570-supply-gap-korea-2026` (신조 공백)
- `shadow-fleet-1300-tankers-supply-removal-freight-floor-korea-2026` (그림자 함대 회피 수법)
- `vlcc-fleet-korea-crude-oil-supply-2026` (선령·중고선가)
- 셋 다 한국어, 같은 숫자, 같은 결론. **같은 언어·같은 시장이라 크로스언어 건과 성격이 다르다**
- 셋 다 현재 「크롤링됨 – 색인 안 됨」 87개 안에 있다. 구글이 하나만 남기고 버렸을 가능성
- 요약은 각도를 갈라 작성. **본문 정리는 미해결로 남긴다**

## [2026-08-04] 작업 | 발행글 61편 요약(excerpt) 전량 작성·입력
- 배경: 61편 전부 요약 공란 → 378 스니펫이 본문 첫 문단을 잘라 meta description으로 출력. 문장 중간에서 `…` 로 끊긴 채 검색 결과에 노출되고 있었다
- 6차에 걸쳐 10/10/10/10/10/11편. 각 155자 이내 (378이 160자 초과 시 157자에서 절단)
- 영문 글 6편은 영문 요약으로 작성
- 지침 [14] 제한 표현(「들이는 기름」)은 요약에서 제외
- 검증: 슬러그별 meta description 조회 → 전 편이 `…` 없이 마침표 종결 확인
- 전체 61편 목록은 WP REST 페이징으로 확정 (사이트맵 61편과 일치)
